server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    location = /community { try_files /community.html =404; }
    location /post/ { try_files $uri /post.html; }
    location /profile/ { try_files $uri /profile.html; }
    location /assets/ { expires 7d; }
}
