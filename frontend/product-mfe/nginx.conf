server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    location /product/ {
        try_files $uri /product.html;
    }
    location /bundle/ {
        try_files $uri /bundle.html;
    }
    location /assets/ {
        expires 7d;
    }
}
