apiVersion: v1
kind: ConfigMap
metadata:
  name: community-mfe-config
  namespace: pawradise
data:
  default.conf: |
    server {
      listen 80;
      server_name _;
      root /usr/share/nginx/html;
      index index.html;

      location / {
        try_files $uri $uri/ /index.html;
      }

      location = /health {
        return 200 "OK";
        add_header Content-Type text/plain;
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: community-mfe
  namespace: pawradise
  labels:
    app: community-mfe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: community-mfe
  template:
    metadata:
      labels:
        app: community-mfe
    spec:
      containers:
      - name: community-mfe
        image: registry.local/pawradise-community-mfe:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/http.d
          readOnly: true
        resources:
          requests:
            memory: "32Mi"
            cpu: "10m"
          limits:
            memory: "64Mi"
            cpu: "50m"
      volumes:
      - name: nginx-config
        configMap:
          name: community-mfe-config
---
apiVersion: v1
kind: Service
metadata:
  name: community-mfe
  namespace: pawradise
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: community-mfe
  type: ClusterIP
