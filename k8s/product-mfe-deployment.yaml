apiVersion: v1
kind: ConfigMap
metadata:
  name: product-mfe-config
  namespace: pawradise
data:
  default.conf: |
    server {
      listen 80;
      server_name _;
      root /usr/share/nginx/html;
      index index.html;

      location / {
        try_files $uri $uri/ /index.html;
      }

      location = /health {
        return 200 "OK";
        add_header Content-Type text/plain;
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-mfe
  namespace: pawradise
  labels:
    app: product-mfe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-mfe
  template:
    metadata:
      labels:
        app: product-mfe
    spec:
      containers:
      - name: product-mfe
        image: registry.local/pawradise-product-mfe:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/http.d
          readOnly: true
        resources:
          requests:
            memory: "32Mi"
            cpu: "10m"
          limits:
            memory: "64Mi"
            cpu: "50m"
      volumes:
      - name: nginx-config
        configMap:
          name: product-mfe-config
---
apiVersion: v1
kind: Service
metadata:
  name: product-mfe
  namespace: pawradise
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: product-mfe
  type: ClusterIP
