apiVersion: apps/v1
kind: Deployment
metadata:
  name: commerce-service
  namespace: pawradise
  labels:
    app: commerce-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: commerce-service
  template:
    metadata:
      labels:
        app: commerce-service
    spec:
      containers:
      - name: commerce-service
        image: registry.local/pawradise-commerce-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8083
        env:
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: pawradise-config
              key: DB_HOST
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: pawradise-config
              key: DB_PORT
        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: pawradise-config
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: pawradise-secrets
              key: DB_PASSWORD
        - name: DB_NAME
          value: "commerce_db"
        - name: REDIS_HOST
          valueFrom:
            configMapKeyRef:
              name: pawradise-config
              key: REDIS_HOST
        - name: IDENTITY_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: pawradise-config
              key: IDENTITY_SERVICE_URL
        - name: PAYMENT_SERVICE_URL
          valueFrom:
            configMapKeyRef:
              name: pawradise-config
              key: PAYMENT_SERVICE_URL
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: pawradise-config
              key: LOG_LEVEL
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /ready
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: commerce-service
  namespace: pawradise
spec:
  ports:
  - port: 8083
    targetPort: 8083
  selector:
    app: commerce-service
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: commerce-service-hpa
  namespace: pawradise
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: commerce-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
