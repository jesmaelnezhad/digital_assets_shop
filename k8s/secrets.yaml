apiVersion: v1
kind: Secret
metadata:
  name: pawradise-secrets
  namespace: pawradise
type: Opaque
stringData:
  DB_PASSWORD: "change-me-in-production"
  JWT_SECRET: "change-me-in-production-use-openssl-rand-hex-32"
  ADMIN_TOKEN: "change-me-admin-access-token"
---
# Sealed Secrets template (for use with kubeseal)
# apiVersion: bitnami.com/v1alpha1
# kind: SealedSecret
# metadata:
#   name: pawradise-secrets
#   namespace: pawradise
# spec:
#   encryptedData:
#     DB_PASSWORD: AgBy8hT4...  # kubeseal --format yaml < secrets.yaml > sealed-secrets.yaml
#     JWT_SECRET: AgCtr5...
#     ADMIN_TOKEN: AgAuv6...
