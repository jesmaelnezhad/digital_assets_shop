apiVersion: v1
kind: ConfigMap
metadata:
  name: shop-mfe-config
  namespace: store4bots
data:
  default.conf: |
    server {
      listen 80;
      server_name _;
      root /usr/share/nginx/html;
      index index.html;

      location / {
        try_files $uri $uri/ /index.html;
      }

      location = /health {
        return 200 "OK";
        add_header Content-Type text/plain;
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-mfe
  namespace: store4bots
  labels:
    app: shop-mfe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shop-mfe
  template:
    metadata:
      labels:
        app: shop-mfe
    spec:
      containers:
      - name: shop-mfe
        image: registry.local/store4bots-shop-mfe:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/http.d
          readOnly: true
        resources:
          requests:
            memory: "32Mi"
            cpu: "10m"
          limits:
            memory: "64Mi"
            cpu: "50m"
      volumes:
      - name: nginx-config
        configMap:
          name: shop-mfe-config
---
apiVersion: v1
kind: Service
metadata:
  name: shop-mfe
  namespace: store4bots
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: shop-mfe
  type: ClusterIP
